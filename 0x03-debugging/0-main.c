#include "main.h"
/**
 * main - tests function that prints if integer is positive or negative
 * Return: 0
 * positive_or_negative - calling this function
 */
int main(void)
{
	int i = 0;

	positive_or_negative(i);

	return (0);
}
