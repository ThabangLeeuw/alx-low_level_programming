#include "main.h"
/**
 * _islower - Tells us how many characters are in lowercase
 * Return: 0
 * Written by Imraan ThabangLeeuw
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1)
	}
	else
	{
		return (0);
	}
}
